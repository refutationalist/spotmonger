# Maintainer: Sam Mulvey <github@refutationalist.com>
pkgname=spotmonger
pkgver=1.3.1
pkgrel=1
pkgdesc="broadcast cart-player oriented audio player"
arch=('x86_64' 'i686')
url="http://sam.vis.nu/"
license=('GPL')
groups=('studmufn')
depends=('nwjs' 'php' 'ffmpeg' 'tar' 'mplayer')
changelog=
replaces=('cartamoc')
conflicts=('cartamoc')
source=(spotmonger.tgz)
noextract=(spotmonger.nw)
install="spotmonger.install"



build() {
	echo "I like pie."
}


package() {
	cd $pkgdir

	cp -r ${srcdir}/mk/* .
	chown -R root:root $pkgdir
	chmod 755 ${pkgdir}/usr/bin/spotmonger
	
}
